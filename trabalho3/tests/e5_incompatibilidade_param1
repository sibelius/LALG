program p5;
var a,b,c : integer;
procedure meu_proc(x: integer);
begin
    x := 10;
end;
begin
    a := 10;
    b := 20;
    c := 30;
    meu_proc(a);
    meu_proc(a;b); {Mais parametros que o esperado}
end.
